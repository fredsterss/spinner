

/**
 * Expose Spinner
 */
module.exports = Spinner;



function Spinner (el, options, callback) {
  if (!(this instanceof Spinner)) return new Spinner(el, options, callback);
  // r.ui.spinner = Spinner;
  // r.ui.spinner.settings = lines: 9, length: 3, width: 2, radius: 4, trail: 50;
}